using Xunit;

namespace NcrunchAvaloniaDemo.Tests
{
    public class SampleTest
    {
        [Fact]
        public void SampleFact()
        {
            // The test code doesn't matter. The build fails without the "Additional files to include" setting.
            Assert.True(false);
        }
    }
}